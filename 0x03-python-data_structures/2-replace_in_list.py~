#!/usr/bin/python3
if __name__ == "__main__":

    def replace_in_list(my_list, idx, element):
        if idx < 0:
            return (my_list)

        length = len(my_list)


elif idx > length - 1:
    return (my_list)

else:
    return(my_list.insert(3, new_element))
